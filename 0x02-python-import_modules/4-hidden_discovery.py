#!/usr/bin/python3
if --name-- == "--main--":
    import hidden_4
    args = dir(hidden_4):

        for i in range(len(args)):
            if args[i].startswith("__")
            continue
        print("{}".format(arhs[i]))
